# Custom ignores
backup_extension.sh
backups/
tests/
COPILOT_INSTRUCTIONS.md
notes.txt

# Operating System Files
.DS_Store
Thumbs.db
ehthumbs.db
.Spotlight-V100
.Trashes

# IDE and Editor Files
.idea/             # IntelliJ IDEA (if you ever use it)
.vscode/           # VS Code specific files and settings
*.swp              # Vim swap files
*~                 # Backup files created by some editors

# Node.js and JavaScript Specific
node_modules/      # Node.js dependency directory
npm-debug.log*     # npm debug logs
yarn-debug.log*    # Yarn debug logs
yarn-error.log*    # Yarn error logs
package-lock.json  # Generated by npm for consistent installs (often ignored in libraries, but sometimes committed in applications)
pnpm-lock.yaml     # Generated by pnpm for consistent installs

# Environment Variables
.env               # Common file for environment variables
.env.*             # Other environment variable files

# Build Artifacts and Output (adjust based on your build process)
dist/              # Common output directory for built files
build/             # Another common output directory
coverage/          # Code coverage reports
*.log              # Any log files

# Chrome Extension Specific (if your build process creates a specific temporary or output folder)
# For example, if you have a 'build' folder for the final extension or a 'temp' folder during development:
# build_extension_output/
# temp_extension_files/

# Other common ignores
.project           # Eclipse project files (if applicable)
.settings/         # Eclipse project settings (if applicable)